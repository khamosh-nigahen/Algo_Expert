/**
 * A tree is a type of graph which is a root, it is directed, acyclic, connected and have only one parent.
 * 
 * A Binary tree is one which has atmost 2 child nodes, note:- leaf node can have a single child (remember term atmost 2 child)
 * 
 * Some trees can be indirectional as well, means it can point to its parent node as well
 */


